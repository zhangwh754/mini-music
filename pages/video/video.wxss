/* pages/video/video.wxss */
/* 隐藏scroll-view滚动条 */
::-webkit-scrollbar {
  width: 0;
  height: 0;
  color: transparent;
}

.navScroll {
  display: flex;
}

.navItem {
  padding: 5rpx 30rpx;
  font-size: 28rpx;
  height: 60rpx;
  line-height: 60rpx;
  flex: 0 0 auto;
}

.active {
  border-bottom: 1px solid red;
}
